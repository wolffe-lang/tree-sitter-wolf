==================
plain string is an f-string
==================

let s = "hello, {who}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (interpolation
          (identifier))))))

==================
interpolation holds a real expression
==================

let s = "{a.b(c)}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (interpolation
          (call_expression
            (field_expression
              (identifier)
              (identifier))
            (arguments
              (argument
                (identifier)))))))))

==================
interpolation nests a string which interpolates again
==================

let s = "{ "wrap-{inner}-wrap" }"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (interpolation
          (string_literal
            (interpolation
              (identifier))))))))

==================
brace escapes are literal
==================

let s = "{{not interpolated}}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (brace_escape)))))

==================
escape sequences
==================

let s = "a\nb\t\"\\\x7f\u{1F43A}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (escape_sequence)
        (escape_sequence)
        (escape_sequence)
        (escape_sequence)
        (escape_sequence)
        (escape_sequence)))))

==================
format spec after the first top-level colon
==================

let s = "{m["k"]:>8}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (interpolation
          (index_expression
            (identifier)
            (string_literal))
          (format_spec))))))

==================
format spec width itself interpolated
==================

let s = "{n:>{w}}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (string_literal
        (interpolation
          (identifier)
          (format_spec
            (interpolation
              (identifier))))))))

==================
triple-quoted string terminates at the next triple quote
==================

let poem = """
    the wolf runs
    the moon watches
    """

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (multiline_string_literal
        (string_content)))))

==================
triple-quoted: lone and double quotes are content, not terminators
==================

let s = """say "hi" and ""hi"" but not thrice"""

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (multiline_string_literal
        (string_content)))))

==================
triple-quoted string interpolates
==================

let s = """
    hello {who}
    """

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (multiline_string_literal
        (string_content)
        (interpolation
          (identifier))
        (string_content)))))

==================
raw string: no interpolation, no escapes
==================

let s = r"no\nescape {not_interp}"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (raw_string_literal
        (string_content)))))

==================
raw string fences balance
==================

let one = r#"say "hi" there"#
let two = r##"fence #" inside"##

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (raw_string_literal
        (string_content))))
  (let_declaration
    (binder
      (identifier)
      (raw_string_literal
        (string_content)))))

==================
generalized literal is a prefixed raw body
==================

let vowels = re"[aeiou]+"
let cfg = path"/etc/hosts"

---

(source_file
  (let_declaration
    (binder
      (identifier)
      (generalized_string_literal
        (identifier)
        (string_content))))
  (let_declaration
    (binder
      (identifier)
      (generalized_string_literal
        (identifier)
        (string_content)))))
